# Phase 5 — Experiment G1.2: isotropic ν by GEOMETRIC DISTORTION alone (k=1 fixed)

**Question.** Achieve Goal 1 (isotropic Poisson ratio ν, E free) with **almost no k variation** — here
**k = 1 fixed for every bond**, so the *only* design variable is **vertex positions** — and see how much
of the isotropic ν range in [−1, 1] can be recovered by geometric **distortion**, across **10 genuinely
distinct triangulated topologies** (connectivity classes). "Topology" = connectivity class: a triangular
or *deformed*-triangular lattice is the **same** topology; a crystalline lattice with a basis is a
**different** topology. Every edge is a real uniform spring (no soft "fictional" edges — that would be
huge k-variation).

## Method
- **Design variable:** node positions only; **k ≡ 1**. **Connectivity frozen** per topology
  (`positions.spsa_positions(..., redelaunay_every=0)` — no re-Delaunay / no flips during a run).
- **Optimizer:** derivative-free SPSA on positions, started from a **symmetry-breaking jitter** (a
  perfect lattice is a stationary point → zero gradient → SPSA stalls; jitter gives it a direction),
  2 restarts spanning step size (`a` = 0.15 and 0.25); keep the candidate minimising the SCORE
  `err + 0.5·gap` — target accuracy and solver-vs-sim agreement WEIGHED, not one vetoing the other.
  A geometric degeneracy floor (`quality_floor = 1e-3`) rejects sliver-producing SPSA steps in the
  loop. *(Both changed 2026-08-21/22; the prior rule and its consequences are in "Key findings".)*
- **Targets:** 11 isotropic ν in [−0.95 … +0.9], flat ν(θ); E left free (E-weight 0). Isotropy tracked
  via std of ν(θ) (`nu_aniso_std`, small throughout).
- **Verification:** every kept design independently simulated (a different code path from the solver).
- A geometry **health gate** screens each distorted candidate before the sim, because a near-singular
  distortion HARD-CRASHES the scipy/LAPACK sim (native segfault, uncatchable in Python).
- Sleep-prevention enabled (a prior overnight run was killed by system sleep).

**Run:** 110 designs (10 topologies × 11 ν); **45 clear the solver-vs-sim agreement gate**, and all
110 are reported — a rejected design is data, not an absence.

> **RE-RUN 2026-08-22 under the SCORED selection** (`run_g1_2.py`, `err + 0.5·gap`, the gap a cost
> rather than a veto); solver side recovered by `g1_2_solver_recheck.py` into `solver_recheck.npz`.
> Every number below comes from that run. Two earlier states are void: the July figures predate the
> August forward-map fixes (A-0, A-10), and the 2026-08-18 re-run used the old veto selection, which
> reported `triangular` as reaching only +0.038. **Read the correction block in "Key findings" before
> quoting anything from this document.** Trustworthy counts: July 52 → 08-18 45 → now **45 of 110**
> (unchanged — the scored selection recovered the *reach*, not the agreement).

## The 10 topologies — all coordination signatures DISTINCT
(from `topologies.csv`; degree:fraction)

| topology (class) | n | coord signature | undistorted ν |
|---|---|---|---|
| triangular (bravais) | 56 | `6:1.00` | +0.333 |
| honeycomb (basis) | 64 | `3..9` mixed | +0.234 |
| kagome (basis) | 54 | `4:.13\|5:.30\|6:.31\|7:.11\|8:.07\|10:.07` | +0.238 |
| square_octagon 4.8.8 (tiling) | 36 | `3..8` mixed | +0.082 |
| tetrakis / union-jack (basis) | 72 | `4:.50\|8:.50` | +0.333 |
| rotating_squares (auxetic) | 32 | `5:.22\|6:.56\|7:.22` | +0.285 |
| reentrant_honeycomb (auxetic) | 64 | `4:.11\|5:.22\|6:.33\|7:.19\|8:.16` | +0.273 |
| foam_poisson (foam) | 54 | `5:.31\|6:.41\|7:.24\|8:.04` | +0.308 |
| flipped_tri_f8 (flipped) | 56 | `4:.02\|5:.18\|6:.62\|7:.14\|8:.04` | +0.291 |
| flipped_tri_f14 (flipped) | 56 | `4:.07\|5:.23\|6:.38\|7:.27\|8:.05` | +0.258 |

*(Signatures from the 2026-08-18 `topologies.csv`; undistorted ν = `nu_initial` under the corrected
solver — these shifted from the July values, which is the A-0/A-10 fix showing up on the same meshes.)*

("square + one diagonal" is just a sheared triangular lattice = the triangular topology, so the honest
distinct square-crystalline class used is **tetrakis/union-jack** `4:.50|8:.50`. The old `flipped_tri_f16`
was dropped — 16 flips produced a near-singular network with ν≈646; replaced by validated `f14`.)

## Reachable isotropic ν per topology
Reference: eta-disordered triangular lattices (uniform k), **frozen 6-6-6 connectivity, Phase-2
convention** (`build_periodic_tf_mesh`: triangulate-first, displace every vertex by a random-direction
vector of magnitude exactly η, never re-triangulate, η<0.5), span ν ∈ **[−0.115, +0.327]** — falling
from +⅓ and crossing into **auxetic (~−0.1) as η→0.48**. (This corrects an earlier mistaken band of
[+0.26, +0.33], which used too-weak a displacement, capped η at 0.30, and wrongly re-triangulated.)
Note this reference is an UNFILTERED sim number — no gap test is applied to it — so it may only be
compared against the sim column below, never against the trustworthy window.

> **Read the two columns separately.** "Most auxetic reached" is what the network *does*: the
> independent sim's ν, with the solver's own ν beside it. "Trustworthy window" is the sub-range where
> the two code paths additionally agree to `gap < 0.05`. **They answer different questions**, and
> reporting only the second is what made an earlier version of this document claim that distortion
> never reaches negative ν.

| topology | undistorted ν | most auxetic reached — sim / solver | trustworthy window | n trust |
|---|---|---|---|---:|
| honeycomb | +0.234 | **−0.436** / −0.488 | [+0.101, +0.311] | 6 |
| rotating_squares | +0.285 | **−0.398** / −0.416 | [+0.101, +0.342] | 6 |
| square_octagon | +0.082 | **−0.336** / −0.471 | none | 0 |
| foam_poisson | +0.308 | **−0.273** / −0.315 | [+0.201, +0.340] | 5 |
| flipped_tri_f8 | +0.291 | **−0.272** / −0.403 | [+0.200, +0.333] | 5 |
| kagome | +0.238 | **−0.245** / −0.328 | [+0.100, +0.339] | 6 |
| tetrakis | +0.333 | **−0.226** / −0.303 | [+0.202, +0.333] | 5 |
| triangular | +0.333 | **−0.217** / −0.304 | [+0.102, +0.334] | 6 |
| flipped_tri_f14 | +0.258 | **−0.215** / −0.409 | [+0.100, +0.332] | 6 |
| reentrant_honeycomb | +0.273 | **−0.197** / −0.359 | none | 0 |

**ALL TEN topologies reach auxetic ν, and the solver and the sim agree on the sign for every one of
them** — 49 designs reach ν < 0, **49/49 auxetic in BOTH code paths**, median |ν_solver − ν_sim| =
0.073 (max 0.195). Overall reach **[−0.436, +0.342]**; deepest is honeycomb at ν = −0.436 (solver
−0.488), with k identically 1 — far beyond the η-disorder band's −0.115.

**What the gap means here, in absolute ν** (`solver_recheck.npz`; the gate is relative *per angle*,
so the headline gap number overstates the discrepancy when |ν| is small):

| gap bin | n | median \|Δν\| bulk | worst-angle | median \|ν\| |
|---|---|---|---|---|
| [0.00, 0.05) | 45 | 0.0000 | 0.0000 | 0.310 |
| [0.05, 0.20) | 18 | 0.0069 | 0.0193 | 0.237 |
| [0.20, 0.50) | 20 | 0.0364 | 0.0615 | 0.241 |
| [0.50, 1.00) | 21 | 0.1117 | 0.1386 | 0.205 |
| ≥ 1.00 | 6 | 0.1545 | 0.2556 | 0.150 |

So the disagreement at the deep end is real — order 0.1 in ν, not a scaling artefact — but it is
about *how* auxetic, never *whether*.

Within the trustworthy window the reachable range is [+0.100, +0.342], median |ν − ν*| over all
110 runs = 0.112.

## Key findings
> ### ⚠ CORRECTION 2026-08-21 — twice-corrected; read this before the numbers above
> **Round 1 (July → August).** The July run reported auxetic reach (−0.09 triangular, −0.21 `f14`)
> from data produced *before* the A-0 shear-contraction and A-10 ν-convention fixes. Those particular
> numbers are void.
>
> **Round 2 (the reporting error).** The first August rewrite of this document then concluded
> "no topology reaches negative ν". **That was wrong**, and wrongly propagated into `Phase 5/PLAN.md`
> and `validation_2026-08/MANIFEST.md`. It came from tabulating only rows that passed
> `trustworthy` — and `trustworthy` is *not* a physicality verdict. It is
> `gap = max_θ|Δν|/(|ν_sim|+0.05) + max_θ|ΔE|/|E_sim| < 0.05`: a **relative, per-angle agreement
> test between two code paths**. At |ν| ≈ 0.44 a 0.09 disagreement at ONE angle scores 0.19 and fails,
> while the bulk values agree to 0.03.
>
> **Measured truth (2026-08-22 re-run):** **49** designs reach ν < 0, **49/49 auxetic in BOTH code
> paths**, on **all ten** topologies. The July *conclusion* was right even though its numbers were not.
>
> **Round 3 (the selection rule).** Even the 08-18 re-run understated reach, because `design_one`
> kept the closest-to-target *trustworthy* candidate and fell back to the lowest-gap one — which
> preferred timid designs. Replacing that with a score (`err + 0.5·gap`) moved `triangular` from
> **+0.038 to −0.217**, `flipped_tri_f8` from −0.057 to −0.272, `tetrakis` from −0.067 to −0.226 and
> `rotating_squares` from −0.259 to −0.398. Nothing about the physics changed; the search had already
> found these designs and the selection was discarding them.
>
> **Why the old rule failed, measured** (`g1_2_triangular_start_probe.py`, 36 runs, this experiment's
> budget, varying only the start): the controlling variable is the SPSA **step size**, not the start.
> Every run reaching deep negative ν used `a = 0.25`; the `a = 0.15` runs stayed positive
> (+0.141 / +0.058 / +0.025 across seeds) — and, decisively, `a = 0.15` returns the **same** endpoint
> for ν* = −0.10 and −0.30, i.e. it is target-independent. That is exactly the saved-data signature:
> one byte-identical geometry (sha1 `3b5ca445f57d`) written for all five of `triangular`'s negative
> targets. The timid design scored gap 0.031 and passed the veto; the `a = 0.25` design reached
> ν = −0.134 at gap 0.371 and was thrown away. All three starts tested — including the default
> jitter — beat the η-disorder reference of −0.109; starting *from* η-disorder reached −0.220.
> The figures now draw every design — filled = trustworthy, hollow = gap > 0.05 with a bar showing
> the disagreement — so this class of error cannot recur silently.

1. **Positions alone (uniform k, fixed connectivity) DO reach auxetic ν — on every topology tried.**
   With k ≡ 1 exactly and connectivity frozen, distortion reaches **ν = −0.436** (honeycomb,
   independent sim; solver −0.488) and ν < 0 on **10 of 10** topologies. This is far beyond the random η-disorder reference, which only
   crosses zero at the singular edge (−0.115 at η = 0.48): **directed** distortion reaches a much
   deeper auxetic regime, and reaches it on demand — which is the point of optimizing over randomizing.
   What the honesty gate rejects is not the auxeticity; it is the *per-angle* agreement of the two
   codes on ν(θ) once |ν| is large.
2. **Universal upper cap ν ≈ +1/3.** Every topology stalls near +0.30–0.33 on the high side — the
   isotropic maximum cannot be raised by distortion, regardless of topology.
3. **Reach is topology-dependent, and the ordering is not the mechanism-prone one.** Deepest are
   `honeycomb` (−0.436) and `rotating_squares` (−0.398) — and `rotating_squares` has a full
   trustworthy window (6 designs), so "deepest reach" and "no trustworthy design" are **not** the
   same set, as an earlier draft of this document claimed. `square_octagon` (−0.336) and
   `reentrant_honeycomb` (−0.197) are the two with no trustworthy design, and they sit third and
   *last* in reach. The flipped variants are mid-pack (`f8` −0.272, `f14` −0.215), so the July claim
   that non-Delaunay flipping specifically opens the auxetic direction remains unsupported.
4. **The honesty gate marks where the two CODES diverge — not where the material stops.** Pushing ν
   further drives the network toward a near-mechanism, and there the solver's per-triangle metric
   solve and the sim's nodal solve stop agreeing on ν(θ): 65 of 110 runs are rejected. But the
   agreement they lose is *directional and relative* — on the rejected auxetic designs the two still
   agree on the SIGN of ν in 49/49 auxetic designs (median bulk |Δν| = 0.073). **2** topologies
   (`reentrant_honeycomb`, `square_octagon`) have no design that clears the gate at all, and those two
   are also among the deepest-reaching — the correlation to note is gate-failure with *large* |ν|, not
   with unreality. Which code to believe there is open. A third, ANALYTIC path does exist
   (`test_hex_closed_form.py`, independent of both solver and sim), but only for the hexagon family —
   it cannot adjudicate these disordered cells until A-18 generalises it to arbitrary basis cells.
   Note also that the obvious suspect does **not** survive testing: `rcond(A(s))` alone is not a
   predictor of solver error — on the hexagon family, driving `A(s)` to numerical rank-1 with soft
   spokes *improves* agreement with the closed form by six orders
   (`hex_conditioning_check.py`, corr(log10 rcond, log10|Δν|) = **+0.73**).

## Limitations / next
- SPSA budget (2 restarts × 120 steps) is modest; more restarts would tighten reachable ranges and may
  recover some trustworthy designs on the "none" topologies.
- Extreme ν (|ν| → 1) is **not** reachable by distortion + uniform k — that regime needs stiffness
  contrast or genuine mechanisms (see Goal 1: contrast is the auxeticity lever).
- The mechanism-prone topologies would benefit from a small k-contrast allowance or a stability term.

## Figures (`results/g1_2/`)
- `fig1_reachable_by_topology.png` — reachable-ν interval per topology, undistorted ν marked, eta-band overlaid.
- `fig2_achieved_vs_target.png` — achieved (sim) vs target ν, colored by topology.
- `fig3_curves_per_topology.png` — ν_target → ν_achieved per topology.
- `fig4_montage.png` — distorted networks at their extreme reachable ν.

Data: `results.csv`, `results.npz`, `topologies.csv`, `eta_reference.npz`; designs (add-only) in
`networks/g1_2/`.
